{
    "configurations": [
        
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Maze Game",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}