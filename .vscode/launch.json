{
    "configurations": [
        
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Maze Game",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}

// python -m http.server 8000

// http://localhost:8000/index.html